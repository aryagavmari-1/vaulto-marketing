{
  "$comment": "GENERATED FILE — do not edit by hand. Produced by scripts/claims/sync-proof-table.mjs from the ARY-23 §0 proof table (the single source of truth for product claims). To retire a claim: update the §0 row and its claims-guard entry in ARY-23, then run `npm run claims:sync`.",
  "source": "ARY-23 #document-positioning-deck §0",
  "sourceRevisionId": null,
  "sourceNote": "Seeded from ARY-23 §0 at deck revision a6b7f9af (claims C-001..C-023). The machine-readable claims-guard block is not in ARY-23 yet — until the CMO adds it, `npm run claims:sync` will fail by design rather than regenerate from a source that does not exist. Verified by dry run (CLAIMS_DECK_FILE) against the live deck: all 12 retired claims accounted for.",
  "generatedAt": "2026-07-28T00:00:00Z",
  "retiredClaimsInTable": [
    "C-002",
    "C-003",
    "C-005",
    "C-007",
    "C-010",
    "C-012",
    "C-015",
    "C-018",
    "C-020",
    "C-021",
    "C-022",
    "C-023"
  ],
  "rules": [
    {
      "id": "isolation-quantifier",
      "claim": "C-002",
      "label": "Per-user isolation stated as a universal quantifier",
      "patterns": [
        "\\bon every (request|endpoint)\\b",
        "\\bevery (endpoint|api endpoint)\\b",
        "\\b(isolat|enforc|check|verif|authenticat)\\w*[^.\\n]{0,60}\\bevery (request|endpoint)\\b"
      ],
      "guidance": "assertVaultOwnership() covers the vault routes, but routes/objects.ts serves stored photos and scans via an HMAC-signed capability URL with no per-user check. The quantifier is what is false, not the isolation.",
      "instead": "each person's vault is isolated from other users"
    },
    {
      "id": "revocable-sharing",
      "claim": "C-003",
      "label": "Consent-based, revocable sharing",
      "patterns": [
        "\\brevocab\\w*",
        "\\brevok\\w+\\b[^.\\n]{0,60}\\baccess\\b",
        "\\bconsent[- ]based\\s+sharing\\b",
        "\\bwithdraw\\w*\\s+(consent|access)\\b"
      ],
      "guidance": "Dead claim. referrals.ts exists only as a compiled leftover in dist/, data_consents has zero source hits, and the only revokedAt in the tree is on refresh tokens. Nothing in the product can be un-shared.",
      "instead": "invite someone into a capture session, and remove them (C-004) — or export, which is a file you hand over and therefore is not revocable (C-017)"
    },
    {
      "id": "audit-trail",
      "claim": "C-005",
      "label": "Audit trail / activity log",
      "patterns": [
        "\\baudit\\s+(trail|log)s?\\b",
        "\\bactivity\\s+log\\b",
        "\\baccess\\s+log\\b",
        "\\bwho\\s+(viewed|accessed)\\b[^.\\n]{0,30}\\bwhen\\b"
      ],
      "guidance": "logAudit() in api-server/src/lib/audit.ts has zero callers, so audit_events is always empty. A table is not a feature.",
      "instead": "nothing — there is no user-visible history of access"
    },
    {
      "id": "networth-snapshot",
      "claim": "C-007",
      "label": "Monthly / stored net-worth snapshots",
      "patterns": [
        "\\bnet[\\s-]?worth\\b[^.\\n]{0,25}\\bsnapshots\\b",
        "\\bnet[\\s-]?worth\\s+(history|histories)\\b",
        "\\b(monthly|automatic(?:ally)?|scheduled)\\s+net[\\s-]?worth\\s+(snapshot|history)",
        "\\bmonthly\\b[^.\\n]{0,40}\\bnet[\\s-]?worth\\b[^.\\n]{0,40}\\b(snapshot|history)\\b",
        "\\bnet[\\s-]?worth\\b[^.\\n]{0,40}\\b(snapshot|history)\\b[^.\\n]{0,40}\\b(monthly|each month|every month)\\b"
      ],
      "guidance": "netWorthSnapshots.ts does not exist and there is no monthly cadence anywhere. The total is computed live by aggregateAssetValues(); only per-asset valuation history is stored.",
      "instead": "your net worth, recalculated as you update what you own — and valuation history per asset"
    },
    {
      "id": "share-link",
      "claim": "C-012",
      "label": "Time-limited share links",
      "patterns": [
        "\\btime[- ]limited\\s+(share|sharing)\\s+links?\\b",
        "\\bshare(able)?\\s+links?\\b",
        "\\bsharing\\s+links?\\b",
        "\\bexpiring\\s+links?\\b"
      ],
      "guidance": "No shareLink, shareToken, or share route exists. getSignedDownloadUrl() is the internal capability URL that serves images to the app — it is not a user-facing way to send someone a link. Note the guard deliberately does NOT match \"links that expire\": describing how our own storage serves your files is accurate (C-012 says so). What is dead is the noun — a share link you hand to another person.",
      "instead": "invite someone into a capture session (C-004), or export (C-017)"
    },
    {
      "id": "referral-package",
      "claim": "C-015",
      "label": "Professional referral packages",
      "patterns": [
        "\\breferral\\s+(package|packet|bundle)s?\\b",
        "\\breferral\\s+recipients?\\b",
        "\\b(send|deliver|share)\\w*\\b[^.\\n]{0,40}\\bto\\s+(your|a)\\s+(solicitor|advisor|adviser|accountant)\\b[^.\\n]{0,30}\\bdirectly\\b"
      ],
      "guidance": "referral_packages and referral_recipients have zero source hits. The only residue is an analytics event name; lead_profiles is real but internal, with no delivery path to a professional.",
      "instead": "export a clean copy you can hand to your solicitor or advisor (C-017)"
    },
    {
      "id": "bulk-export-roadmap",
      "claim": "C-017",
      "label": "Bulk file download described as unbuilt",
      "patterns": [
        "\\bbulk\\s+(download|export)\\b[^.\\n]{0,60}\\b(roadmap|coming soon|not yet|planned)\\b",
        "\\b(download|export)\\w*\\b[^.\\n]{0,60}\\b(original|uploaded)\\s+files?\\b[^.\\n]{0,40}\\b(roadmap|coming soon|not yet)\\b"
      ],
      "guidance": "Stale under-claim, not an over-claim. routes/dataExport.ts streams a ZIP containing data.json plus the actual uploaded files — it shipped, and saying otherwise undersells us on the trust page.",
      "instead": "export a ZIP archive that includes your original uploaded photos and scans alongside the structured record"
    },
    {
      "id": "capture-sprint-count",
      "claim": "C-010",
      "label": "Capture sprints undercounted as four",
      "patterns": [
        "\\b(four|4)\\s+(capture\\s+)?sprints?\\b",
        "\\bcapture\\s+sprints?\\b[^.\\n]{0,30}\\b(four|4)\\b"
      ],
      "guidance": "There are FIVE capture sprint types, not four — room / category / 20 things / documents / partner session. The partner-session sprint has a full UI card (ARY-1359); saying four undersells a shipped feature.",
      "instead": "five capture sprints — room, category, 20 things, documents, and a partner session"
    },
    {
      "id": "free-tier-paid-fields",
      "claim": "C-023",
      "label": "Paid-report sections presented as free-tier output",
      "patterns": [
        "\\brisk\\s+areas?\\b",
        "\\brecommended\\s+actions?\\b",
        "\\bactions?\\s+to\\s+take\\b",
        "\\basset\\s+breakdown\\b",
        "\\brisk\\s+projections?\\b",
        "\\bfree\\b[^.\\n]{0,60}\\b(overview|estimate|advisory)\\b[^.\\n]{0,80}\\b(considerations|next steps)\\b",
        "\\b(considerations|next steps)\\b[^.\\n]{0,60}\\b(free|no cost)\\b[^.\\n]{0,40}\\b(overview|estimate)\\b"
      ],
      "guidance": "These are fields of the PAID AdvisoryReport (advisoryPipeline.ts:133-145). The free AdvisoryEstimate carries only ranges, lineItems, costOfInaction and gapsInProtection — it has no riskAreas and no recommendedActions. This is a tier boundary, not a capability denial: the feature exists, it just is not free. Bare \"considerations\" and \"next steps\" are ordinary English and are only matched next to free-tier wording; the rest are literal field names and are matched anywhere. ARY-1369: sweep the phrase FAMILY over the CORPUS — a one-term grep closed ARY-1364 green while \"recommended actions to take\" was still live.",
      "instead": "free tier: \"estimate ranges\", \"where your protection has gaps\", \"the cost of leaving things as they are\", \"what a detailed report would add\". Deeper tier: \"the full report\" — never a price."
    }
  ],
  "notPatterned": [
    {
      "reason": "A stale channel count (11 vs 13), not a retired phrase. No regex expresses 'the number is wrong'; this row is enforced by the deck's list of real channel names.",
      "claim": "C-013"
    },
    {
      "reason": "\"zero-knowledge\" and \"end-to-end encrypted\" appear legitimately and frequently as DENIALS in our honesty copy — /security says we deliberately do not claim them, in all 16 locales, and 15 blog posts repeat the disclaimer. A phrase-level regex would fire on the honest disclaimer far more often than on a real over-claim, which is the 'worse than nothing' failure mode this guard was built to avoid. Enforced by Brand & Trust review.",
      "claim": "C-020"
    },
    {
      "reason": "Quantifier row (\"everything is encrypted\"). Expressible as a regex, but there is a known live hit at src/content/blog/important-family-documents-to-keep.md that must be corrected first — promoting this to a rule before that fix would land a red build. Tracked as follow-up to ARY-1340; promote in the same edit as the copy fix.",
      "claim": "C-021"
    },
    {
      "claim": "C-018",
      "reason": "\"bank-grade\" / \"military-grade\" / SOC 2 / ISO. Same denial problem as C-020: the blog argues against grade adjectives by name (\"adjectives aren't evidence; mechanisms are\"), so the strings appear in copy whose whole point is that we do not claim them. Enforced by Brand & Trust review."
    },
    {
      "claim": "C-022",
      "reason": "\"bank-grade\" / \"military-grade\" / SOC 2 / ISO. Same denial problem as C-020: the blog argues against grade adjectives by name (\"adjectives aren't evidence; mechanisms are\"), so the strings appear in copy whose whole point is that we do not claim them. Enforced by Brand & Trust review."
    }
  ]
}
