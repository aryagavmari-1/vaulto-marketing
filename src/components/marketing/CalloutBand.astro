---
/**
 * §3.7 CalloutBand — full-bleed card band with hairline top/bottom. Two named
 * slots: `left` (headline + CTA) and `right` (proof list / content).
 */
---
<div class="callout">
  <div class="wrap">
    <div><slot name="left" /></div>
    <div><slot name="right" /></div>
  </div>
</div>
