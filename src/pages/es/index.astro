---
/**
 * Spanish Home (ARY-407, Path A). Mirrors the English Home one-for-one; all copy
 * comes from the `es` overlays in `i18n/home.ts` + `i18n/ui.ts`. Adding this file
 * + the `'/': ['en','es']` entry in `src/config/i18n.ts` is the whole publish step.
 */
import HomePage from '../../components/HomePage.astro';
---
<HomePage locale="es" />
