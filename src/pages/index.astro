---
/**
 * English Home (default locale). The page body lives in the locale-aware
 * `HomePage` component (ARY-413); a localized Home is now a one-liner —
 * e.g. `src/pages/es/index.astro` renders `<HomePage locale="es" />` and the
 * copy comes from the `es` overlays in `i18n/home.ts` + `i18n/ui.ts`.
 */
import HomePage from '../components/HomePage.astro';
---
<HomePage locale="en" />
