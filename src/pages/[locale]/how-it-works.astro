---
/** Localized How it works (ARY-427). One route → every non-default locale. */
import { NON_DEFAULT_LOCALES, type Locale } from '../../config/i18n';
import HowItWorksPage from '../../components/pages/HowItWorksPage.astro';

export function getStaticPaths() {
  return NON_DEFAULT_LOCALES.map((locale) => ({ params: { locale } }));
}

const { locale } = Astro.params as { locale: Locale };
---
<HowItWorksPage locale={locale} />
